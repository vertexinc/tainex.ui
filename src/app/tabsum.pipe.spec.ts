/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TabsumPipe } from './tabsum.pipe';

describe('Pipe: Tabsum', () => {
  it('create an instance', () => {
    let pipe = new TabsumPipe();
    expect(pipe).toBeTruthy();
  });
});
