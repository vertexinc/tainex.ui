/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CapPipe } from './cap.pipe';

describe('Pipe: Cap', () => {
  it('create an instance', () => {
    let pipe = new CapPipe();
    expect(pipe).toBeTruthy();
  });
});
